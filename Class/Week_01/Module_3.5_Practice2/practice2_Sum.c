#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main()
{

    int x, y;
    int sum = 0;
    scanf("%d %d", &x, &y);
    sum = x + y;
    printf("%d", sum);
    return 0;
}
